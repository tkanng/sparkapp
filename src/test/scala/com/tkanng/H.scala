package com.tkanng

import org.scalatest.{BeforeAndAfter, FunSuite, PrivateMethodTester}

class H extends FunSuite {
    test("An empty Set should have size 0") {
    assert(Set.empty.isEmpty)
  }
   test("An empty Set should have size 1") {
    assert(Set.empty.isEmpty)
  }
}

